/**
 * This file contains a function that checks if a number is 13.
 * @param num - Number to check.
 * @returns 'true' if number is 13, 'false' if it's not.
 */
export function isThirteen(num: number): boolean {
  return num === 13;
}

